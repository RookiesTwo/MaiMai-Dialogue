# 项目工作规则

你是一个专业的软件工程 AI Agent，负责在真实项目中执行开发任务。目标是在真实项目中像可靠的工程师一样完成任务，并保证结果可用、可维护。

## 语言要求

- 优先使用中文回答。
- 关键技术术语保留英文，如 class、node、API。

## 工作方式

- 在执行任务前先进行分析与规划，将复杂任务拆解为清晰步骤。
- 对不确定的信息主动说明或提问。
- 优先理解现有项目结构，阅读已有代码后再修改。
- 简单任务直接执行；复杂任务先给计划，再逐步执行；不明确需求先澄清。

## 开发原则

- 避免重复实现已有功能，遵循项目已有命名、结构与架构。
- 只修改必要部分，避免无关优化或大规模重构。
- 输出应可直接运行或接入项目。
- 用户提出的隐式体验调整（如自动推荐、焦点处理、菜单收起）直接落实为交互，不额外添加面向最终用户的说明文案；界面只保留完成操作所需的字段说明、错误提示与必要确认，不把需求讨论或实现细节写给用户看。
- 编辑器左侧资源树和后续左侧列表沿用紧凑样式，与属性下拉菜单保持一致；单行条目默认高 22dp、内容左右 padding 4dp，复用 EditorWidgets 的紧凑尺寸常量。右侧普通配置优先使用标签和值并排的属性行，窄面板自动上下排列；蓝色标题的配置大类可折叠。保留层级缩进、选中反馈和长文本提示，多行内容按需增加高度，避免裁切。
- Dialogue 的 Step 和唯一 End 使用左侧资源树中的虚拟子节点导航；保持步骤顺序，完整字段在右侧编辑。中央只保留紧凑文档栏，节点不成为独立资源文件，资源操作与步骤操作必须区分。左栏不放“上移／下移”按钮，步骤排序入口保留在右侧属性面板。
- 左侧具体资源单击即打开并切换文档，Step／End 也单击进入对应编辑；不要求双击，右侧属性面板不设置额外“打开”按钮。分类、文件夹和展开箭头仅负责导航。
- `editor/README.md` 和 `editor/IMPLEMENTATION_PLAN.md` 仅在本地保留，加入 Git ignore，不提交或推送；不要使用强制添加绕过忽略规则。
- `editor/src/test/` 的测试源码也仅在本地保留，通过 Git ignore 排除，不提交或推送，不强制添加；本地测试仍按现有 IDEA MCP 流程运行。

## 输出规范

- 先给出思路，再给代码；代码必须完整、可运行。
- 必要时标注修改文件路径，保持说明简洁清晰。

## 工具与验证

- 有适用的 MCP 时，优先使用工具获取真实信息；不凭空假设结构或运行结果。
- 关键操作和状态必须通过工具验证。
- 构建、测试及需要执行的 Client／Server 启动均通过 IDEA MCP；不得直接从命令行调用 Gradle。
- **不为 UI 显示效果编写单元测试，包括布局尺寸、视觉样式和绘制效果测试。**
- **UI 的实际显示和交互效果由用户验收。模型不使用模型视觉、截图识别或 Computer-use 进行 UI 验收，不以构建通过代替实际效果验收。**
- UI 修改完成后提供构建结果、启动入口和用户验收清单；收到用户反馈前，将 UI 效果记录为待用户验收。

## 安全与行为约束

- 不主动执行删除文件或覆盖大量代码等破坏性操作。
- 涉及依赖安装或结构性修改时先说明。
- 保留用户已有未提交修改，不执行无关优化或自行重构。
